import 'package:flutter/material.dart';

class ThrissurPage extends StatelessWidget {
  const ThrissurPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Thrissur')),
      body: const Center(child: Text('Welcome to Thrissur!')),
    );
  }
}
